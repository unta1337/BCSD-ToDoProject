<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bcsd.todo.repository.UserRepository">
    <resultMap id="userResultMap" type="bcsd.todo.domain.User">
        <result column="id_uniq" property="idUniq" />
        <result column="id" property="id" />
        <result column="password" property="password" />
        <result column="is_valid" property="isValid" />
    </resultMap>

    <select id="getUserByIdUniq" resultMap="userResultMap">
        select * from user where id_uniq = #{id_uniq}
    </select>

    <select id="getUserById" resultMap="userResultMap">
        select * from user where id = #{id}
    </select>

    <select id="getValidUserById" resultMap="userResultMap">
        select * from user where id = #{id} and is_valid = true
    </select>
</mapper>
